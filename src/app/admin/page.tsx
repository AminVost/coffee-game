import { ArrowLeft, Plus } from "lucide-react";
import { StatCard } from "@/components/admin/stat-card";
import { Button } from "@/components/ui/button";
import { Card } from "@/components/ui/card";
import { adminStats, liveMatches, recentRegistrations } from "@/data/mock-data";
export default function AdminDashboard(){return <div><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="section-kicker">ADMIN OVERVIEW</p><h1 className="section-title mt-2">داشبورد مدیریت</h1><p className="mt-2 text-sm text-[var(--muted)]">خلاصه وضعیت مجموعه و مسابقات امروز</p></div><Button href="/admin/tournaments/new"><Plus size={17}/>مسابقه جدید</Button></div><div className="mt-7 grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{adminStats.map(s=><StatCard key={s.title} stat={s}/>)}</div><div className="mt-7 grid gap-6 xl:grid-cols-[1.2fr_.8fr]"><Card className="overflow-hidden"><div className="flex items-center justify-between p-5"><h2 className="font-black">ثبت‌نام‌های اخیر</h2><Button href="/admin/participants" variant="ghost" size="sm">همه <ArrowLeft size={15}/></Button></div><div className="overflow-x-auto"><table className="w-full min-w-[600px] text-right text-sm"><thead className="bg-[var(--surface-2)] text-xs text-[var(--muted)]"><tr><th className="p-4">نام</th><th className="p-4">مسابقه</th><th className="p-4">وضعیت</th><th className="p-4">مبلغ</th></tr></thead><tbody>{recentRegistrations.map(r=><tr key={r.name} className="border-t border-[var(--line)]"><td className="p-4 font-bold">{r.name}</td><td className="p-4 text-[var(--muted)]">{r.tournament}</td><td className="p-4"><span className="rounded-full bg-emerald-500/10 px-3 py-1 text-xs font-bold text-emerald-500">{r.status}</span></td><td className="p-4">{r.amount}</td></tr>)}</tbody></table></div></Card><Card className="p-5"><div className="flex items-center justify-between"><h2 className="font-black">بازی‌های امروز</h2><span className="text-xs text-[var(--muted)]">{liveMatches.length} بازی</span></div><div className="mt-5 grid gap-3">{liveMatches.map(m=><div key={m.id} className="rounded-2xl bg-[var(--surface-2)] p-4"><div className="flex justify-between text-xs text-[var(--muted)]"><span>{m.round}</span><span>{m.resource}</span></div><div className="mt-3 flex items-center justify-between gap-3 text-sm font-black"><span>{m.home}</span><span className="rounded-xl bg-[var(--surface)] px-3 py-2">{m.homeScore??"-"} : {m.awayScore??"-"}</span><span>{m.away}</span></div></div>)}</div></Card></div></div>}
